# Experiments

Each training run records its dataset, architecture, split, epoch budget, device, accuracy, and macro-F1. The UI intentionally separates a reproducible benchmark record from activating a model: a run is not presented as clinically validated until a real checkpoint and reliability report are registered.
